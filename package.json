{
  "name": "figma-desktop-bridge",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "@figma/plugin-typings": "^1.108.0"
  }
}
